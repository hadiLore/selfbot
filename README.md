# 🤖 سلف‌بات تلگرام فارسی (Telethon)

سلف‌بات (Userbot) ماژولار و کامل برای اکانت شخصی تلگرام - بیش از صد دستور در ۲۹ دسته: از ابزارهای روزمره، سرگرمی و فونت گرفته تا هوش مصنوعی، اتوماسیون، مدیریت گروه، جستجوی جهانی و سیستم پلاگین.

نقطه‌ی اجرا `main.py` است و کل منطق در پوشه‌ی `bot/` به‌صورت ماژولار سازمان‌دهی شده (پیشوند پیش‌فرض دستورات: `.`).

## فهرست مطالب

1. [سلف‌بات یعنی چی؟](#۱-سلف‌بات-یعنی-چی)
2. [امکانات در یک نگاه](#۲-امکانات-در-یک-نگاه)
3. [ساختار پروژه](#۳-ساختار-پروژه)
4. [پیش‌نیازها](#۴-پیش‌نیازها)
5. [نصب و راه‌اندازی](#۵-نصب-و-راه‌اندازی)
6. [متغیرهای محیطی](#۶-متغیرهای-محیطی)
7. [راهنمای کامل دستورات](#۷-راهنمای-کامل-دستورات)
8. [پایگاه‌داده و مهاجرت](#۸-پایگاه‌داده-و-مهاجرت)
9. [سیستم پلاگین](#۹-سیستم-پلاگین)
10. [تست‌ها](#۱۰-تست‌ها)
11. [نکات امنیتی و مسئولیت‌پذیری](#۱۱-نکات-امنیتی-و-مسئولیت‌پذیری)
12. [عیب‌یابی رایج](#۱۲-عیب‌یابی-رایج)

---

## ۱) سلف‌بات یعنی چی؟

این اسکریپت با **اکانت شخصی تلگرام خودتون** لاگین می‌شه - نه با یک بات جدا که از BotFather ساخته باشید. `API_ID`/`API_HASH` مستقیماً از پنل رسمی [my.telegram.org](https://my.telegram.org) گرفته می‌شه (همون مکانیزم رسمی‌ای که هر کلاینت شخص‌ثالث تلگرام، مثل خودِ اپ‌های موبایل/دسکتاپ، ازش استفاده می‌کنه) و با کتابخانه‌ی [Telethon](https://docs.telethon.dev) به یک Session وصل می‌شه.

نکات مهم که باید در نظر داشته باشید:

- **فقط پیام‌هایی که خودِ شما (owner) بفرستید** به‌عنوان دستور شناسایی و اجرا می‌شن؛ پیام‌های بقیه هیچ‌وقت دستور حساب نمی‌شن (مگر برای هندلرهای پس‌زمینه‌ای مثل فیلترها که عمداً روی پیام‌های دیگران کار می‌کنن).
- دستورات مدیریت گروه (`اخراج`/`مسدود`/`ارتقا`/`تنزل`/...) رو فقط در گروه‌هایی که خودتون واقعاً ادمین هستید استفاده کنید؛ تلگرام این سطح دسترسی رو خودش در سمت سرور چک می‌کنه.
- قابلیت‌هایی مثل ارسال خودکار (`.ارسال‌خودکار`) و تگ‌کردن همه (`.برچسب‌همه`) می‌تونن در صورت استفاده‌ی افراطی (فاصله‌ی خیلی کم، تکرار زیاد) توسط سیستم‌های ضداسپم تلگرام محدودیت روی اکانت ایجاد کنن؛ مقادیر پیش‌فرض پروژه برای کاهش این ریسک تنظیم شده‌ن (حداقل فاصله، batch کردن، سقف تعداد اعضا) ولی مسئولیت نهاییِ میزان استفاده با خودتونه.
- فایل `.env` یا مقدار `SESSION_STRING` معادل رمز عبور کامل اکانت شماست؛ هرگز جایی منتشرش نکنید.

---

## ۲) امکانات در یک نگاه

| دسته | خلاصه |
|---|---|
| 🛠 ابزار | ماشین‌حساب، QR کد، کوتاه‌کننده‌ی لینک، آب‌وهوا، ترجمه، جستجوی گوگل، پسوردساز، هش، Base64، شمارنده‌ی کاراکتر، نرخ ارز، قیمت لحظه‌ای طلا/ارز/سکه (TGJU) |
| 📝 یادداشت | ذخیره/نمایش/حذف/لیست یادداشت‌های شخصی |
| 🗑 مدیریت پیام | حذف تکی، پاک‌سازی گروهی، پین/آنپین |
| 🎉 سرگرمی | بیش از ۱۵ دستور و بازی (تاس، حدس عدد، اسلات، کوییز، فال حافظ، عشق‌سنج، ...) |
| 🔤 فونت پیام | ۲۷ فونت (لاتین/فارسی/ترکیبی) + اعمال خودکار روی پیام‌های بعدی |
| 👤 پروفایل و ساعت زنده | بیو، نام، عکس، ساعت زنده در نام با ۱۵ مدل |
| 🤖 منشی چت | پاسخ خودکار در نبود شما، تشخیص آنلاین/آفلاین کاملاً محلی، زمان‌بندی ساعتی، پاسخ با AI |
| 👮 مدیریت گروه | kick/ban/mute/promote/demote، قفل گروه، فیلتر لینک، خوش‌آمدگویی، تگ همه، فیلتر پورن (AI Vision)، فیلتر اسپم (محلی) |
| 💾 بکاپ‌گیری | متن/JSON/رسانه/لیست چت‌ها/تنظیمات کامل بات + بازیابی |
| ⚙️ تنظیمات یکپارچه | نمایش و تغییر سریع وضعیت همه‌ی زیرسیستم‌ها از یک‌جا |
| ⚡ موتور اتوماسیون | قانون‌های event → action قابل تعریف توسط خودتون |
| 🔔 مرکز اعلان‌ها | قانون بر اساس کلیدواژه/فرستنده → نوتیف/ذخیره/فوروارد/پاسخ خودکار |
| 📥 اینباکس | ذخیره‌ی دستیِ پیام‌های مهم برای مرور بعدی |
| 🧠 حافظه‌ی AI | حافظه‌ی دسته‌بندی‌شده (کاربران/گفتگوها/پروژه‌ها/یادداشت‌ها/تنظیمات) |
| 👤 پروفایل کاربر | تگ و یادداشت خصوصی روی کاربران دیگر |
| 🔍 جستجوی جهانی | هم‌زمان در داده‌های داخلی بات و در خودِ تلگرام (چت‌های شما + جستجوی عمومی) |
| 💬 پاسخ هوشمند | پیش‌نویس پاسخ با ۴ لحن (رسمی/دوستانه/طنز/کوتاه) با تأیید قبل از ارسال |
| 🩺 سلامت سیستم | وضعیت دیتابیس، ورکرهای پس‌زمینه و uptime |
| 🧩 پلاگین | نصب/حذف/ری‌لود آنیِ کد پایتون از لینک گیت‌هاب، بدون ری‌استارت |
| 📤 ارسال خودکار | ارسال دوره‌ای یک متن به چند گروه انتخابی |
| ⏰ زمان‌بند و یادآوری | پارسر زمان فارسی/انگلیسی (نسبی، ساعت امروز/فردا، تاریخ کامل) |
| 🌙 خلاصه‌ی روزانه | خلاصه‌ی شبانه‌ی خودکار از پیام‌های روز با AI (یا گزارش ساده بدون AI) |
| 🖼 رسانه و فایل | واترمارک، فشرده‌سازی، تبدیل فرمت، استیکرساز، OCR (محلی یا AI) |
| 🤖 هوش مصنوعی | پرسش/خلاصه/ترجمه‌ی هوشمند، هسته‌ی سازگار با API چت‌تکمیلیِ OpenAI |
| 🧠 روتر دستوری هوشمند | تبدیل یک جمله‌ی محاوره‌ای به دستور مناسب، با تأیید قبل از اجرا |
| 🔊 صوت و متن | تبدیل صدا به متن و متن به صدا، کاملاً رایگان و بدون نیاز به AI |
| 📊 آمار | شمارش دستورات/پیام‌ها/خطاها/ارسال‌خودکار به‌تفکیک هر چت |
| 🖥 پنل دکمه‌ای | منوی دکمه‌ای اختیاری از طریق یک بات کمکیِ جدا (چون تلگرام دکمه‌ی شیشه‌ای رو فقط برای بات واقعی نشون می‌ده) |

---

## ۳) ساختار پروژه

```
main.py                          نقطه‌ی اجرا: wiring + client.run_until_disconnected
generate_session.py              اسکریپت یک‌بارِ گرفتنِ SESSION_STRING
requirements.txt                 وابستگی‌های اجرا
requirements-dev.txt             + pytest/pytest-asyncio برای تست
Dockerfile / Procfile / railway.json   دیپلوی (Railway، مبتنی بر Docker)
alembic.ini                      تنظیمات Alembic (مهاجرت اسکیمای دیتابیس)

bot/
  config.py                      همه‌ی تنظیمات از .env (تنها جایی که os.getenv صدا زده می‌شه)
  runtime.py                     نمونه‌ی TelegramClient + بات کمکیِ پنل + سشن HTTP مشترک + SELF_ID
  utils.py                       ساخت الگوی regex دستورات (pat)
  calc.py                        ماشین‌حساب امن (.حساب)
  fonts.py                       نگاشت‌های ۲۷‌گانه‌ی فونت پیام
  clock.py                       ۱۵ استایل ساعت زنده + تسک پس‌زمینه‌ی به‌روزرسانی پروفایل
  ai.py                          هسته‌ی هوش مصنوعی: wrapper سبک روی API سازگار با OpenAI
  automation_engine.py           موتور اجرای قانون‌های اتوماسیون (event → action)
  notification_engine.py         موتور اجرای قانون‌های مرکز اعلان‌ها
  plugin_loader.py               نصب/حذف/ری‌لود آنیِ پلاگین از فایل/لینک
  health.py                      جمع‌آوری و قالب‌بندی گزارش سلامت سیستم
  local_speech.py                رونویسی رایگان (Google STT) و متن‌به‌صوت رایگان (edge-tts)
  logging_config.py              پیکربندی لاگ

  db/                            اتصال PostgreSQL (SQLAlchemy 2.x async + psycopg 3)
    engine.py, models.py, models_ext.py, bootstrap.py

  repositories/                  تنها لایه‌ای که مستقیم SQL/ORM می‌زنه (Repository Pattern)
    notes_repo.py, assistant_repo.py, autopost_repo.py, font_repo.py, clock_repo.py,
    stats_repo.py, scheduler_repo.py, group_guard_repo.py, hafez_repo.py,
    notification_repo.py, automation_repo.py, ai_memory_repo.py, user_profile_repo.py,
    inbox_repo.py, daily_digest_repo.py, settings_repo.py

  storage/                       آداپتورهای async روی Repository Layer + state درون‌حافظه‌ای
    notes_store.py, font_store.py, assistant_store.py, autopost_store.py, clock_store.py,
    stats_store.py, scheduler_store.py, group_guard_store.py, daily_digest_store.py,
    settings_toggles.py, json_file.py

  handlers/                      هر فایل یک دسته از دستورات؛ افزودنِ دستور جدید یعنی فقط
                                  باز کردنِ فایلِ مربوطه (یا فایل جدید + اضافه‌کردن به __init__.py)
    general.py        (۱) عمومی: پینگ/فعال/آیدی/اطلاعات
    tools.py           ابزار: حساب/کیوآر/کوتاه/هوا/ترجمه/جستجو/رمزعبور/هش/بیس۶۴/ارز/قیمت
    notes.py           (۳) یادداشت
    messages.py        (۴) مدیریت پیام: حذف/پاکسازی/سنجاق
    fun.py             سرگرمی و بازی‌ها
    font.py            فونت پیام
    profile.py         (۷) پروفایل: بیو/نام/عکس/ساعت
    assistant.py       منشی چت هوشمند (بزرگ‌ترین هندلر پروژه)
    admin.py           (۹) مدیریت گروه پایه: kick/ban/mute/promote/...
    groupguard.py       مدیریت گروه پیشرفته: فیلترلینک/خوش‌آمد/برچسب‌همه/فیلترپورن/فیلتراسپم
    backup.py           بکاپ‌گیری و بازیابی
    settings_center.py  تنظیمات یکپارچه
    automation.py       موتور اتوماسیون
    notifications.py    مرکز اعلان‌ها
    inbox.py            اینباکس داخلی
    ai_memory.py        حافظه‌ی هوش مصنوعی
    user_profile.py     پروفایل داخلی روی کاربران دیگر
    global_search.py    جستجوی جهانی
    smart_reply.py       پاسخ هوشمند
    health.py            دستور .سلامت
    plugins_cmd.py        دستورات مدیریت پلاگین
    autopost.py           ارسال خودکار متن
    scheduler.py          زمان‌بند و یادآوری
    daily_digest.py       خلاصه‌ی روزانه
    media.py, ocr.py, audio.py   رسانه/OCR/صوت
    ai.py                  دستورات هوش مصنوعی: پرسش/خلاصه/ترجمه‌هوشمند
    command_router.py     روتر دستوری هوشمند (.هوش)
    stats.py               آمار
    help.py                 راهنما
    panel.py                پنل دکمه‌ای (از طریق بات کمکی)
    __init__.py             ثبت/import همه‌ی هندلرهای بالا

migrations/                      مهاجرت‌های Alembic (اسکیمای PostgreSQL)
  versions/0001_initial_schema.py … 0009_assistant_schedule.py

scripts/
  migrate_json_to_postgres.py    انتقال یک‌بارِ JSONهای قدیمی به PostgreSQL (با بکاپ و شمارش قبل/بعد)
  seed_hafez.py                  پرکردنِ جدول hafez_poems برای `.فال` (یک‌بار، از روی سیستم خودتون)

plugins/                         پلاگین‌های داخلیِ (built-in) کنار bot/ - جدا از پلاگین‌های نصب‌شده در runtime
  README.md                      مستندات فرمت پلاگین (انگلیسی)

tests/                           ۹۱۸ خط تست با pytest + pytest-asyncio (۱۳ فایل)
```

---

## ۴) پیش‌نیازها

- **Python 3.12+** (تصویر Docker پروژه از `python:3.12-slim` استفاده می‌کنه)
- یک اکانت تلگرام + `API_ID`/`API_HASH` رایگان از [my.telegram.org](https://my.telegram.org)
- **PostgreSQL** - منبع اصلیِ داده‌های دائمی از این نسخه به بعد (نه فایل JSON)
- برای قابلیت‌های رسانه/OCR: `ffmpeg`، `tesseract-ocr` (+ بسته‌ی زبان فارسی `tesseract-ocr-fas`) و `fonts-dejavu-core` - همه‌شون در `Dockerfile` از قبل نصب شدن؛ اگر بدون Docker اجرا می‌کنید باید خودتون نصبشون کنید.
- (اختیاری) یک توکن بات از [@BotFather](https://t.me/BotFather) برای فعال‌سازیِ پنل دکمه‌ای (`.پنل`)
- (اختیاری) یک `AI_API_KEY` سازگار با فرمت OpenAI برای قابلیت‌های AI

---

## ۵) نصب و راه‌اندازی

### روش ۱: Railway (روش اصلی و پیشنهادی)

چون سرورهای Railway ترمینال تعاملی برای وارد کردن کدِ تاییدِ تلگرام ندارن، مرحله‌ی لاگین (گرفتنِ Session String) باید **اول روی سیستم خودتون** انجام بشه.

**۱. گرفتنِ Session String (یک‌بار، قبل از دیپلوی):**
```bash
pip install telethon python-dotenv
python generate_session.py
```
(`API_ID`/`API_HASH` رو قبلش توی یک فایل `.env` کنارِ `generate_session.py` بذارید.) شماره‌تلفن و کدِ تاییدی که تلگرام می‌فرسته رو وارد کنید؛ در آخر یک رشته‌ی طولانی چاپ می‌شه - همون رو نگه دارید.

**۲. ساختنِ دیتابیس PostgreSQL روی Railway:**
توی همون پروژه‌ی Railway → **New → Database → Add PostgreSQL**. Railway خودش یک متغیر محیطی `DATABASE_URL` می‌سازه و در صورت استفاده از Reference Variables به سرویسِ بات وصلش می‌کنه.

**۳. دیپلوی از طریق گیت‌هاب:**
1. پوشه‌ی پروژه رو به یک ریپوی گیت‌هاب پوش کنید.
2. Railway → **New Project → Deploy from GitHub repo** → ریپو رو انتخاب کنید.
3. Railway به‌خاطرِ `Dockerfile`/`railway.json` خودش بیلد و اجرا رو مدیریت می‌کنه:
   ```
   alembic upgrade head && python main.py
   ```
   یعنی هر دیپلوی/ری‌استارت، اول اسکیمای دیتابیس به‌روز می‌شه (idempotent) و بعد بات روشن می‌شه.

یا از طریق CLI:
```bash
npm i -g @railway/cli
railway login
railway init
railway up
```

**۴. تنظیمِ متغیرهای محیطی** (تب **Variables**):
- `API_ID`, `API_HASH`, `SESSION_STRING` (مرحله‌ی ۱)
- `DATABASE_URL` (اگر با Reference Variable خودکار وصل نشده)
- اختیاری: `PREFIX`, `TIMEZONE_OFFSET`, `CLOCK_INTERVAL`, `BOT_TOKEN`, `AI_API_KEY` (جدول کامل در بخش بعدی)

### روش ۲: Replit (جایگزین)

1. یک Repl از نوع **Python** بسازید و ۴ فایل `main.py`, `requirements.txt`, `generate_session.py`, `.env.example` رو آپلود کنید (پوشه‌های `bot/`، `migrations/` و بقیه هم لازمه کامل منتقل بشن).
2. در Shell: `pip install -r requirements.txt`
3. در تب **Secrets**: `API_ID`, `API_HASH` و اختیاری `PREFIX`/`TIMEZONE_OFFSET`/`CLOCK_INTERVAL`
4. یک‌بار `python generate_session.py` رو اجرا کنید و رشته‌ی خروجی رو با نامِ `SESSION_STRING` به Secrets اضافه کنید.
5. `main.py` رو اجرا (Run) کنید.

⚠️ Replit رایگان بعد از مدتی بی‌فعالیتی خاموش می‌شه؛ برای روشن نگه‌داشتنش معمولاً یک وب‌سرور کوچک (Flask) + پینگ دوره‌ای (مثلاً UptimeRobot) اضافه می‌کنن.

### روش ۳: اجرای محلی با Docker

```bash
docker build -t telegram-selfbot .
docker run --env-file .env telegram-selfbot
```

---

## ۶) متغیرهای محیطی

| متغیر | پیش‌فرض | توضیح |
|---|---|---|
| `API_ID` / `API_HASH` | - | **الزامی** - از my.telegram.org |
| `SESSION_STRING` | خالی | خروجیِ `generate_session.py` |
| `DATABASE_URL` | خالی | **الزامی** - اتصال PostgreSQL |
| `PREFIX` | `.` | پیشوندِ دستورات |
| `BOT_TOKEN` | خالی | برای فعال‌سازیِ `.پنل` (از BotFather) |
| `TIMEZONE_OFFSET` | `3.5` | آفستِ ساعتِ محلی (پیش‌فرض: تهران) |
| `CLOCK_INTERVAL` | `60` | فاصله‌ی به‌روزرسانیِ ساعت زنده (حداقل ۳۰ ثانیه) |
| `DB_POOL_SIZE` / `DB_MAX_OVERFLOW` | `5` / `5` | تنظیمات pool اتصال دیتابیس |
| `DB_POOL_RECYCLE_SECONDS` | `1800` | بازیافتِ اتصال‌های دیتابیس |
| `DB_ECHO` | `false` | لاگ‌کردنِ کامل کوئری‌های SQL |
| `AI_API_KEY` | خالی | کلید سرویسِ سازگار با OpenAI؛ نبودش یعنی قابلیت‌های AI فقط راهنما می‌دن |
| `AI_API_BASE` | `https://api.openai.com/v1` | برای proxy یا مدل self-hosted سازگار |
| `AI_MODEL` | `gpt-4o-mini` | باید از Vision هم پشتیبانی کنه (برای فیلترپورن) |
| `AI_MAX_TOKENS` / `AI_TIMEOUT` | `600` / `30` | سقفِ توکن و timeout درخواست‌های AI |
| `AI_SUMMARY_MAX_MESSAGES` | `300` | سقفِ پیام برای `.خلاصه` |
| `ASSISTANT_HISTORY_LIMIT` | `10` | تعدادِ پیامِ context برای منشیِ AI (فقط درون‌حافظه‌ای) |
| `AI_AUDIO_API_KEY` / `AI_AUDIO_API_BASE` | = مقادیر AI بالا | جدا کردنِ سرویسِ صوتی از سرویسِ چت در صورت نیاز |
| `AI_STT_MODEL` / `AI_TTS_MODEL` / `AI_TTS_VOICE` | `whisper-1` / `tts-1` / `alloy` | فقط برای مسیرِ AI رونویسی/تی‌تی‌اس (نه مسیرِ رایگان) |
| `STT_LANGUAGE` | `fa-IR` | زبانِ رونویسیِ رایگان (Google STT) |
| `TTS_VOICE` | `fa-IR-FaridNeural` | صدای رایگانِ edge-tts (زنانه: `fa-IR-DilaraNeural`) |
| `DAILY_DIGEST_DEFAULT_HOUR` / `_MINUTE` | `23` / `0` | ساعتِ پیش‌فرضِ ارسالِ خلاصه‌ی روزانه |
| `DAILY_DIGEST_MAX_MESSAGES_PER_CHAT` | `150` | سقفِ پیام بررسی‌شده به‌ازای هر چت |
| `DAILY_DIGEST_MAX_CHATS` | `40` | سقفِ تعدادِ چت در حالتِ «کلی» |
| `GROUP_PORN_FILTER_MAX_BYTES` | `8388608` (۸MB) | عکس‌های بزرگ‌تر چک نمی‌شن |
| `GROUP_SPAM_WINDOW_SECONDS` | `10` | بازه‌ی تشخیصِ فلاد |
| `GROUP_SPAM_MAX_MESSAGES` | `6` | سقفِ پیام در بازه قبل از تشخیصِ فلاد |
| `GROUP_SPAM_DUPLICATE_THRESHOLD` | `3` | تعدادِ تکرارِ عینِ متن قبل از حذف |
| `GROUP_SPAM_WARN_COOLDOWN_SECONDS` | `20` | فاصله‌ی هشدارهای فیلتراسپم |
| `ASSISTANT_ONLINE_THRESHOLD` | `180` | ثانیه سکوت تا «آفلاین» حساب شدن |
| `ASSISTANT_CHECK_INTERVAL` | `30` | فاصله‌ی بازبینیِ محلیِ وضعیت آنلاین/آفلاین (حداقل ۱۵) |
| `ASSISTANT_SCHEDULE_MAX_WINDOWS` | `20` | سقفِ بازه‌های زمان‌بندیِ منشی |
| `AUTOPOST_MIN_INTERVAL_MINUTES` | `1` (توصیه: حداقل ۵) | حداقل فاصله‌ی مجاز ارسال خودکار |
| `BACKUP_MAX_MESSAGES` / `BACKUP_MAX_MEDIA` | `2000` / `50` | سقفِ بکاپ‌گیری |
| `NOTES_FILE` / `AUTOPOST_FILE` / `ASSISTANT_FILE` / `FONT_STATE_FILE` / `STATS_FILE` | `*.json` | فقط برای اسکریپتِ مهاجرت و Import/Export دستیِ بکاپ - دیگه در زمانِ اجرا استفاده نمی‌شن |

---

## ۷) راهنمای کامل دستورات

نام‌های انگلیسیِ قدیمی (مثل `.help`, `.ping`) هم برای سازگاری کار می‌کنن؛ زیرِ هر دسته معادلِ لاتینش هم آورده شده.

### 📋 عمومی
```
.پینگ (ping)         تست تأخیر اتصال
.فعال (alive)         وضعیت بات + uptime + وضعیت ساعت زنده
.آیدی (id)            آیدیِ چت/کاربر/پیام (ریپلای اختیاری)
.اطلاعات (info)       اطلاعات کاربر (ریپلای اختیاری، پیش‌فرض خودتون)
```

### 🛠 ابزار
```
.حساب <عبارت>                ماشین‌حساب امن (sqrt, sin, log, pi, ...)
.کیوآر <متن>                  ساخت QR کد
.کوتاه <لینک>                 کوتاه‌کردن لینک
.هوا <شهر>                    آب‌وهوا
.ترجمه <زبان> <متن>           ترجمه (یا با ریپلای: .ترجمه en)
.جستجو <عبارت>                جستجوی گوگل
.رمزعبور <طول اختیاری>        ساخت رمز عبور تصادفی
.هش <الگوریتم اختیاری> <متن>  هش md5/sha1/sha256/sha512 (یا ریپلای)
.بیس۶۴ انکد/دیکد <متن>        تبدیل Base64 (یا ریپلای)
.نویسه‌شمار <متن>             شمارش حروف/کلمات/خط (یا ریپلای)
.ارز <عدد> <از> <به>          تبدیل نرخ ارز، مثلاً .ارز 10 USD IRR
.قیمت                         قیمت لحظه‌ای دلار/یورو/طلا/سکه (منبع: TGJU.org)
.قیمت <طلا/ارز/سکه>           فقط یک بخش
.قیمت <اسم کالا>              جستجوی یک قلمِ خاص، مثلاً .قیمت دلار
```
`.قیمت` از فیدِ JSON عمومیِ TGJU (بدون نیاز به کلید) روی چند زیردامنه (`call1`‑`call4`) استفاده می‌کنه و چند تا رو پشتِ سرِ هم امتحان می‌کنه تا اگه یکی در دسترس نبود بقیه جواب بدن.

### 📝 یادداشت
```
.یادداشت <کلید> <متن>    ذخیره‌ی یادداشت
.یادداشت‌ها               لیستِ یادداشت‌ها
.نمایش‌یادداشت <کلید>     نمایشِ یک یادداشت
.حذف‌یادداشت <کلید>       حذفِ یک یادداشت
```

### 🗑 مدیریت پیام
```
.حذف                    حذفِ پیامِ ریپلای‌شده (و خودِ دستور)
.پاکسازی <عدد>          حذفِ چند پیامِ آخر (یا ریپلای برای حذفِ از همون‌جا تا الان)
.سنجاق / .برداشتن‌سنجاق  پین/آنپینِ پیامِ ریپلای‌شده
```

### 🎉 سرگرمی
```
.تایپ‌زنده <متن>              افکتِ تایپِ زنده حرف‌به‌حرف
.پیش‌تایپ <متن>               شبیه‌سازیِ تایپ قبل از ارسال واقعی
.معکوس <متن>                  معکوس‌کردنِ متن
.طنز <متن>                    حروفِ بزرگ‌وکوچکِ متناوب (طنزآمیز)
.تاس <۱ تا ۶>                 انداختنِ تاسِ واقعی تا رسیدن به همون عدد
.شیرخط                        شیر یا خط
.تصادفی <min> <max>           عددِ تصادفی بینِ دو عدد
.انتخاب <گزینه۱, گزینه۲, ...>  انتخابِ تصادفی بینِ چند گزینه (با کاما یا فاصله)
.سنگ‌کاغذقیچی <گزینه>          بازیِ سنگ‌کاغذقیچی با بات
.حدس شروع <سقف اختیاری>       شروعِ بازیِ حدسِ عدد (پیش‌فرض ۱ تا ۱۰۰)
.حدس <عدد>                    حدس‌زدن (راهنمای بالاتر/پایین‌تر می‌ده)
.حدس لغو                      لغوِ بازیِ فعال
.اسلات                        ماشینِ اسلات با سه نمادِ تصادفی
.جادوگر <سوال>                پاسخِ تصادفیِ توپِ جادویی به سوالِ بله/خیری
.عشق‌سنج <اسم۱> و <اسم۲>       درصدِ عشق‌سنجِ شوخی (برای یک جفتِ ثابت همیشه یکسان)
.این‌یا‌اون                    یک سوالِ «این یا اون» تصادفی
.کوییز                        یک سوالِ عمومیِ چهارگزینه‌ای (Open Trivia DB)
.کوییز <۱ تا ۴>                جواب‌دادن به سوالِ فعال
.فال                          یک فالِ حافظِ تصادفی با تفسیر (از PostgreSQL)
```

### 🔤 فونت پیام
```
.قلم فهرست              لیستِ کاملِ فونت‌ها با نمونه
.قلم <نام> <متن>        تبدیلِ یک‌بارِ متن (یا ریپلای + .قلم <نام>)
.قلم تنظیم <نام>        تنظیمِ فونتِ پیش‌فرض برای حالتِ خودکار
.قلم روشن / خاموش       روشن/خاموش‌کردنِ اعمالِ خودکارِ فونت روی همه‌ی پیام‌های بعدی
.قلم وضعیت              وضعیتِ فعلی
```
سه دسته (۲۷ مورد): **لاتین** (۱۰ فونت از بلاکِ یونیکدِ Mathematical Alphanumeric Symbols)، **فارسی** (۱۳ فونت - سه‌تاشون یعنی `fa_kashida`/`fa_underline`/`fa_strike` واقعاً روی شکلِ حروف اثر می‌ذارن، بقیه تزئینی‌ان)، و **ترکیبی** (۴ فونت). چون تلگرام پیام رو اول واقعی می‌فرسته و بعد ادیت می‌کنه، در حالتِ خودکار یک لحظه‌ی کوتاه متنِ بدونِ فونت قابل‌دیدنه - این محدودیتِ ذاتیِ همه‌ی سلف‌بات‌هاست.

### 👤 پروفایل و ساعت زنده
```
.بیو <متن>            تغییرِ بیو
.نام <متن>            تغییرِ نامِ پایه (زیربنای ساعتِ زنده)
.عکس                  تغییرِ عکسِ پروفایل (ریپلای روی عکس)
.ساعت روشن / خاموش    روشن/خاموش‌کردنِ ساعتِ زنده در نام
.مدل‌ساعت              لیستِ ۱۵ مدلِ موجود
.مدل‌ساعت <نام>/بعدی   تغییرِ مدل
```

### 🤖 منشی چت (پاسخ خودکار هوشمند)
```
.منشی روشن / خاموش              روشن/خاموشِ دستی (تشخیصِ خودکار غیرفعال می‌شه)
.منشی خودکار                    برگشت به تشخیصِ خودکارِ آنلاین/آفلاین
.منشی وضعیت                     نمایشِ وضعیت
.منشی متن <متن>                 تنظیمِ پیامِ پاسخ
.منشی تأخیر <ثانیه>              تنظیمِ تأخیر
.منشی حالت <خودکار/منشن/پیوی/گروه‌ها>   تعیینِ حالتِ پاسخ
.منشی مستثنی / شامل             حذف/افزودنِ چتِ فعلی به استثناها
.منشی پاک                       حذفِ لیستِ چت‌ها
.منشی زمان‌بندی افزودن <شروع> <پایان> [برچسب]   بازه‌ی «حتماً روشن» (مثلاً خواب)
.منشی زمان‌بندی حذف/پاک/روشن/خاموش
.منشی هوش‌مصنوعی روشن / خاموش    پاسخِ AI به‌جای متنِ ثابت (نیازمندِ AI_API_KEY)
```
تشخیصِ آنلاین/آفلاین کاملاً محلی و بدونِ تماس با تلگرام انجام می‌شه (هر چند ثانیه چک می‌کنه آخرین پیامِ واقعیِ شما از هر دستگاهی کِی بوده)، پس هیچ‌وقت ریت‌لیمیت نمی‌شه. در حالتِ متنِ ثابت به هر نفر فقط یک‌بار در هر نشست جواب داده می‌شه؛ در حالتِ AI این محدودیت برداشته می‌شه (چون هر پاسخ مخصوصِ همون پیامه) و هر پاسخِ AI یک واترمارکِ محوِ اسپویلی در انتها داره.

### 👮 مدیریت گروه (فقط جایی که ادمین هستید)
```
.اخراج / .مسدود / .رفع‌مسدود / .ارتقا / .تنزل     با ریپلای روی کاربر
.بی‌صدا <دقیقه اختیاری> / .رفع‌سکوت                با ریپلای روی کاربر
.ادمین‌ها                                          لیستِ ادمین‌های گروه
.لینک‌گروه                                         گرفتنِ لینکِ دعوت
.قفل‌گروه / .بازکردن‌گروه                           قفل/بازکردنِ ارسالِ پیام برای اعضای عادی
.فیلترلینک روشن/خاموش/وضعیت                        حذفِ خودکارِ پیام‌های حاویِ لینک از غیرادمین‌ها
.خوش‌آمد روشن/خاموش/وضعیت/متن <متن>                خوش‌آمدگویی به عضوِ جدید ({نام}/{منشن})
.برچسب‌همه <متن اختیاری>                           تگ‌کردنِ همه (batch ۵تایی، فقط زیر ۲۰۰ عضو)
.فیلترپورن روشن/خاموش/وضعیت/تست                    حذفِ خودکارِ عکسِ نامناسب با AI Vision
.فیلتراسپم روشن/خاموش/وضعیت                        حذفِ فلاد/تکرارِ پیام - کاملاً محلی، بدونِ AI
```
`.فیلترپورن` نیازمندِ `AI_API_KEY` با مدلی است که از ورودیِ تصویر پشتیبانی کنه؛ اگه کلید نباشه یا مدل Vision نداشته باشه، فیلتر **fail-open** می‌کنه (به‌جای حذفِ اشتباهیِ عکسِ سالم، کاری نمی‌کنه). دستورِ `.فیلترپورن تست` با ریپلای روی یک عکس، پاسخِ خامِ AI یا خطای دقیق رو مستقیم نشون می‌ده - برای عیب‌یابیِ سریع بدونِ نیاز به لاگ سرور.

### 💾 بکاپ‌گیری
```
.پشتیبان <عدد>          بکاپِ متنیِ N پیامِ آخر به Saved Messages
.پشتیبان json <عدد>     همون، خروجیِ JSON ساختاریافته
.پشتیبان رسانه <عدد>    دانلود و فوروارد رسانه‌های N پیامِ آخر
.پشتیبان چت‌ها           لیستِ کاملِ چت‌ها/گروه‌ها/کانال‌های اکانت
.پشتیبان تنظیمات        بکاپِ کاملِ تنظیماتِ بات (یادداشت/منشی/ارسالِ‌خودکار/فونت/ساعت/آمار)
.بازیابی                (با ریپلای روی فایلِ خروجیِ «پشتیبان تنظیمات») بازگردانیِ همه‌چیز
```
سقفِ پیش‌فرض ۲۰۰۰ پیام برای بکاپِ متنی/JSON و ۵۰ فایل برای بکاپِ رسانه. `.بازیابی` فقط با ریپلای‌کردنِ خودِ owner روی پیامِ outgoing کار می‌کنه، پس کسی جز خودتون نمی‌تونه از راه دور تنظیماتتون رو تغییر بده.

### ⚙️ تنظیماتِ یکپارچه
```
.تنظیمات                              نمایشِ وضعیتِ زنده‌ی همه‌ی زیرسیستم‌ها
.تنظیمات تنظیم <key> <true|false>     روشن/خاموش‌کردنِ مستقیمِ یک بخش (مثلاً assistant_mode false)
.تنظیمات ذخیره                        خروجیِ JSON از وضعیتِ فعلی
```
(فیلترهای گروه اینجا نیستن چون سراسری نیستن؛ با `.فیلترلینک`/`.خوش‌آمد` داخلِ خودِ گروه تنظیم می‌شن.)

### ⚡ موتور اتوماسیون
```
.اتوماسیون                                              لیستِ قوانین
.اتوماسیون جدید <نام> <رویداد> <عملیات> <مقدار>          مثال: .اتوماسیون جدید سلام message reply سلام 👋
.اتوماسیون فعال/غیرفعال <id>                            روشن/خاموش‌کردنِ یک قانون
.اتوماسیون حذف <id> / اطلاعات <id>
```
رویدادها: `message, schedule, command, user_join, user_leave` | عملیات: `reply, ai, note, schedule, notify, guard, backup, autopost`
⚠️ **فعلاً فقط رویدادِ `message` واقعاً trigger می‌شه** (روی هر پیامِ ورودی از هر چتی). بقیه‌ی رویدادها (`schedule`/`command`/`user_join`/`user_leave`) ذخیره می‌شن ولی هنوز به منبعِ رویدادِ خودشون وصل نشدن - یک جهتِ توسعه‌ی طبیعی برای این بخش، وصل‌کردنِ همین‌هاست.

### 🔔 مرکز اعلان‌ها
```
.اعلان                                          لیستِ قوانین
.اعلان جدید <نام> <نوع> <مقدار> <عملیات>         مثال: .اعلان جدید vip keyword VPN notify
.اعلان فعال/غیرفعال/حذف/اطلاعات <id>
```
نوع: `message, keyword, user` (شناسه‌ی عددیِ فرستنده) | عملیات: `notify` (پیام به خودتون)، `save` (ذخیره در اینباکس)، `forward` (فوروارد به خودتون)، `reply` (پاسخِ خودکار در همون چت)
⚠️ نوعِ `time` فعلاً پشتیبانی نمی‌شه (نیازمندِ زمان‌بندِ جداگانه‌ست).

### 📥 اینباکس
```
.ذخیره                                (با ریپلای) ذخیره در اینباکسِ داخلی؛ اختیاری: مهم/فوری
.اینباکس                              نمایش (یا مهم/خوانده/نخوانده/پاک <id>)
```

### 🧠 حافظه‌ی AI
```
.حافظه                                    آمارِ حافظه
.حافظه افزودن <دسته> <کلید> <مقدار>
.حافظه جستجو/لیست/حذف/پاک <آرگومان>
```
پنج دسته: **کاربران، گفتگوها، پروژه‌ها، یادداشت‌ها، تنظیمات** - برای استفاده‌ی بعدی توسطِ `.پرسش`/`.منشی هوش‌مصنوعی`.

### 👤 پروفایلِ کاربر
```
.کاربر <شناسه اختیاری>              نمایشِ پروفایلِ داخلیِ آن کاربر (بدونِ آرگومان = خودتون)
.برچسب <شناسه> <برچسب>              افزودنِ تگ
.یادداشت‌کاربر <شناسه> <متن>        یادداشتِ خصوصی روی یک کاربر
```

### 🔍 جستجوی جهانی
```
.جستجو <عبارت>
```
هم‌زمان توی: یادداشت‌ها، حافظه‌ی AI، پروفایلِ کاربران، اینباکس، کارهای زمان‌بندی‌شده (داده‌ی داخلیِ بات)، **و** پیام‌های خودتون در تلگرام (چت‌هایی که عضوشونید) و کانال/گروه/کاربرهای عمومیِ کلِ تلگرام (با `SearchGlobalRequest`/`ContactsSearchRequest` تلتون) جستجو می‌کنه. هر بخش try/except جدای خودش رو داره تا اگه یکی fail بشه (مثلاً FloodWait) بقیه‌ی نتایج از بین نره.

### 💬 پاسخ هوشمند
```
.جواب <رسمی|دوستانه|طنز|کوتاه>    (با ریپلای) پیش‌نویسِ پاسخ با AI
.جواب ارسال                       ارسالِ همون پیشنهاد
```
نیازمندِ `AI_API_KEY`. پیشنهادِ تولیدشده تا ۱۲۰ ثانیه در حافظه نگه داشته می‌شه و بعدش منقضی می‌شه - چیزی خودکار ارسال نمی‌شه.

### 🩺 سلامتِ سیستم
```
.سلامت
```
وضعیتِ دیتابیس و ورکرهای پس‌زمینه (زمان‌بند/ارسالِ‌خودکار/منشی/آمار) + uptime.

### 🧩 پلاگین‌ها
```
.پلاگین                                          لیستِ پلاگین‌های فعال
.پلاگین نصب <لینکِ فایلِ .py در گیت‌هاب>          دانلود + بررسیِ حجم/UTF-8/سینتکس + بارگذاریِ آنی
.پلاگین حذف <نام>
.پلاگین reload <نام>                              Unload و Load دوباره بدونِ ری‌استارتِ کلِ بات
```
جزئیاتِ کامل در بخشِ [سیستم پلاگین](#۹-سیستم-پلاگین).

### 📤 ارسال خودکار متن
```
.ارسال‌خودکار                    نمایشِ وضعیتِ کامل
.ارسال‌خودکار روشن / خاموش
.ارسال‌خودکار فاصله <دقیقه>
.ارسال‌خودکار متن <متن>          (یا ریپلای)
.ارسال‌خودکار افزودن / حذف       افزودن/حذفِ گروهِ فعلی از مقصدها
.ارسال‌خودکار پاک                پاک‌کردنِ همه‌ی مقصدها
.ارسال‌خودکار فوری               ارسالِ فوری برای تست
```
⚠️ فاصله‌های خیلی کم (زیرِ ۵ دقیقه) و پیامِ تکراری ریسکِ تشخیصِ اسپم رو بالا می‌برن؛ فقط توی گروه‌های خودتون استفاده کنید. آمارِ موفق/ناموفق با `.آمار` قابلِ مشاهده‌ست.

### ⏰ زمان‌بندی و یادآوری
```
.زمان‌بند <زمان> <متن>       ارسالِ متن سرِ وقت در همین چت (یا ریپلای)
.زمان‌بند لیست / لغو <id>
.یادآوری <زمان> <متن>        یادآوریِ شخصی به Saved Messages (یا ریپلای)
.یادآوری لیست / لغو <id>
```
فرمتِ زمان: نسبی (`10m` `2h` `1d` `45s` یا فارسی `10دقیقه` `2ساعت`)، ساعتِ امروز/فردا (`14:30`)، یا تاریخ کامل (`2026-08-25 14:30`). یک تسکِ پس‌زمینه هر ۱۵ ثانیه چک می‌کنه چیزی سررسیده یا نه؛ همه در PostgreSQL ذخیره می‌شن.

### 🌙 خلاصه‌ی روزانه
```
.خلاصه‌روز                          وضعیت
.خلاصه‌روز روشن / خاموش
.خلاصه‌روز حالت کلی / سفارشی        همه‌ی چت‌ها یا فقط چت‌های انتخابی
.خلاصه‌روز زمان <HH:MM>
.خلاصه‌روز افزودن/حذف [آیدیِ چت] / لیست / پاک
.خلاصه‌روز الان                     اجرای فوری برای تست
```
هر شب سرِ ساعتِ مشخص (پیش‌فرض ۲۳:۰۰ محلی)، خلاصه‌ای از پیام‌های همون روز به Saved Messages می‌فرسته - ترکیبی از موتورِ خلاصه‌سازیِ همان‌که `.خلاصه` استفاده می‌کنه، الگویِ تسکِ پس‌زمینه‌ایِ scheduler، و مقصدِ Saved Messages مرکزِ اعلان. حالتِ «کلی» تا سقفِ ۴۰ چت رو بررسی می‌کنه (برای جلوگیری از هزینه/تایم‌اوتِ AI)؛ اگر `AI_API_KEY` تنظیم نشده باشه، به‌جای خلاصه‌ی هوشمند یک گزارشِ ساده (تعدادِ پیام + چندخطِ آخر) برای هر چت می‌فرسته - قابلیت کاملاً غیرفعال نمی‌شه.

### 🖼 رسانه و فایل
```
.واترمارک <متن>          روی عکسِ ریپلای‌شده واترمارکِ متنی می‌زنه
.فشرده‌سازی               کوچیک‌کردنِ عکس/ویدیویِ ریپلای‌شده
.تبدیل <فرمت>             تبدیلِ فرمتِ فایلِ ریپلای‌شده (مثلاً .تبدیل png روی webp)
.استیکر <نامِ‌پک> <ایموجی اختیاری>   ساختِ استیکر از عکسِ ریپلای‌شده
.استخراج‌متن              OCR روی عکسِ ریپلای‌شده (پیش‌فرض: محلی/رایگان، فارسی+انگلیسی)
.استخراج‌متن <en/fa>      فقط یک زبانِ خاص (محلی)
.استخراج‌متن ai           OCR با AI Vision (بهتر برای دست‌نویس/کیفیتِ پایین)
```

### 🤖 هوش مصنوعی
```
.پرسش <سوال>                    سوال از مدلِ زبانی
.پرسش (با ریپلای)               خلاصه/تحلیلِ همان پیام، یا سوال درباره‌اش (صوتی خودکار رونویسی می‌شه)
.خلاصه <عدد>                    خلاصه‌ی N پیامِ آخرِ همین چت (پیش‌فرض ۵۰)
.ترجمه‌هوشمند <زبان> <متن>       ترجمه با مدلِ زبانی (کیفیتِ بهتر برای متنِ محاوره‌ای/پیچیده)
.منشی هوش‌مصنوعی روشن / خاموش
```
همه از `bot/ai.py` استفاده می‌کنن: یک wrapper سبک روی `/chat/completions` سازگار با OpenAI - یعنی هم خودِ OpenAI جواب می‌ده، هم هر سرویسِ دیگه (proxy یا مدلِ self-hosted) که همون فرمت رو پیاده کنه.

### 🧠 روترِ دستوریِ هوشمند
```
.هوش <جمله‌ی آزاد>    مثال: .هوش فردا ساعت ۹ یادم بنداز به علی زنگ بزنم
.هوش تایید            اجرای پیشنهادِ تشخیص‌داده‌شده
.هوش لغو
```
یک جمله‌ی محاوره‌ای می‌گیره و با AI تشخیص می‌ده کدوم دستور مدنظرتونه. بخش‌های پشتیبانی‌شده: یادآوری/زمان‌بند، یادداشت‌ها، ابزار (محاسبه/ترجمه/تبدیلِ‌ارز/پسورد)، پروفایل (بیو/نام)، فونت، و چندتا دستورِ سرگرمی. مدیریتِ گروه/فایل/اتوماسیونِ دائمی **عمداً پوشش داده نشدن** (مستقیم از دستورِ خودشون استفاده کنید). قبل از هر اجرایی خلاصه‌ی تشخیص‌داده‌شده رو نشون می‌ده و منتظرِ تاییدِ صریح می‌مونه - چیزی خودکار انجام نمی‌شه.

### 🔊 صوت و متن
```
.رونویسی                   (با ریپلای روی ویس) تبدیل به متن
.رونویسی <سوال/درخواست>    رونویسی + پرسیدنِ همون سوال از AI (نیازمندِ AI_API_KEY)
.متن‌به‌صوت <متن>           تبدیلِ متن به ویس (یا ریپلای روی پیامِ متنی)
```
رونویسی از موتورِ رایگانِ گوگل (`SpeechRecognition`) و متن‌به‌صوت از `edge-tts` استفاده می‌کنه - **بدونِ نیاز به `AI_API_KEY`**، فقط اتصال به اینترنت لازمه (و ffmpeg، که در Dockerfile نصبه).

### 📊 آمار
```
.آمار              خلاصه‌ی کلی: دستورات، پیام‌ها، ارسالِ‌خودکار، خطاها، uptime + پراستفاده‌ترین‌ها
.آمار چت‌ها         به‌تفکیکِ هر چت
.آمار بازنشانی     پاک‌کردنِ شمارنده‌ها
```
هر ۶۰ ثانیه یک اسنپ‌شاتِ اتمیک در PostgreSQL ذخیره می‌شه. «تعدادِ خطاها» فقط خطاهای سیستمی/پس‌زمینه رو می‌شمره، نه ورودیِ اشتباهِ خودتون به یک دستور.

### راهنما و پنل
```
.راهنما / .help       همینِ راهنما (در ۳ پیام، به‌خاطرِ سقفِ ۴۰۹۶ کاراکتریِ تلگرام)
.پنل                  منویِ دکمه‌ای (نیازمندِ BOT_TOKEN + Inline Mode فعال از BotFather)
```
تلگرام دکمه‌های شیشه‌ای رو فقط برای پیامِ ارسالی از طرفِ یک بات واقعی نشون می‌ده، نه پیامِ اکانتِ شخصی. برای همین `.پنل` از یک بات کمکیِ جدا استفاده می‌کنه؛ اجرای واقعیِ دستورها همچنان با اکانتِ خودتون انجام می‌شه - بات کمکی فقط نمایش/ناوبری می‌ده. بدونِ `BOT_TOKEN`، `.پنل` فقط یک پیامِ راهنما می‌ده و بقیه‌ی سلف‌بات عادی کار می‌کنه.

---

## ۸) پایگاه‌داده و مهاجرت

از این نسخه به بعد **PostgreSQL منبعِ اصلیِ همه‌ی داده‌های دائمیه** (یادداشت‌ها/منشی/ارسالِ‌خودکار/آمار/ساعتِ زنده/اتوماسیون/اعلان/اینباکس/حافظه‌ی AI/پروفایلِ کاربر/خلاصه‌ی روزانه)، نه فایلِ JSON. مهاجرتِ اسکیما با Alembic مدیریت می‌شه:

```
migrations/versions/
  0001_initial_schema.py
  0002_scheduled_jobs.py
  0003_group_guard.py
  0004_assistant_ai_mode.py
  0005_new_features.py
  0006_hafez_poems.py
  0007_group_filters.py
  0008_daily_digest.py
  0009_assistant_schedule.py
```

دستورِ `alembic upgrade head` قبل از هر اجرای بات (در Dockerfile/Procfile) خودکار صدا زده می‌شه.

**انتقالِ داده‌های قدیمیِ JSON:** اگر از نسخه‌ی JSON این پروژه استفاده می‌کردید:
```bash
python scripts/migrate_json_to_postgres.py
```
این اسکریپت قبل از هر کاری از فایل‌های JSON بکاپ می‌گیره، تعدادِ رکوردها رو قبل/بعد مقایسه می‌کنه، و اگر چیزی مطابقت نداشت بدونِ پاک‌کردنِ چیزی متوقف می‌شه (idempotent - چندبار قابل‌اجراست).

**راه‌اندازیِ `.فال`:** دیوانِ حافظ در جدولِ `hafez_poems` نگه داشته می‌شه. برای پرکردنِ این جدول، **فقط یک‌بار** (بعد از `alembic upgrade head`):
```bash
pip install hafez
python scripts/seed_hafez.py
```
پکیجِ `hafez` فقط برای همین اسکریپت لازمه و نیازی نیست روی سرویسِ اصلی نصب بمونه.

---

## ۹) سیستم پلاگین

بات از نصب، حذف و ری‌لودِ آنیِ پلاگین (بدونِ ری‌استارت) پشتیبانی می‌کنه.

فرمتِ یک پلاگین (همون الگوی Telethon که هندلرهای داخلی استفاده می‌کنن):
```python
from bot.runtime import client
from telethon import events

@client.on(events.NewMessage(outgoing=True, pattern=r"^\.hello$"))
async def hello(event):
    await event.edit("Hello from plugin ✅")

commands = ["hello"]

async def startup():
    pass

async def shutdown():
    pass
```

- برای Unload تمیز، لودر تابع‌های callback تعریف‌شده توسطِ پلاگین رو ردیابی می‌کنه؛ اگر پلاگین هندلر رو پویا ثبت می‌کنه، اون callbackها باید در لیستِ سطحِ ماژولِ `handlers` باشن.
- **پایداری روی Railway:** چون کانتینرهای Railway تضمین نمی‌کنن فایل‌های نوشته‌شده در زمانِ اجرا بعدِ یک دیپلویِ جدید باقی بمونن، برای پلاگین‌های نصب‌شده‌ی دائمی یک Railway Volume به `/data` وصل کنید و `PLUGIN_INSTALL_DIR=/data/plugins` رو ست کنید. پوشه‌ی داخلیِ `plugins/` (پلاگین‌های built-in) از این جدا می‌مونه، پس Volume باعثِ مخفی‌شدنِ built-inها نمی‌شه.
- **امنیت:** یک پلاگینِ پایتونی با همون سطحِ دسترسیِ خودِ سلف‌بات اجرا می‌شه. فقط کدی رو نصب کنید که بهش اعتماد دارید. لودر حجم/UTF-8/سینتکسِ پایتون رو قبل از بارگذاری چک می‌کنه، ولی این‌ها **sandbox نیستن**.

---

## ۱۰) تست‌ها

پروژه یک مجموعه‌تستِ ۹۱۸ خطی با `pytest` + `pytest-asyncio` (حالتِ `asyncio_mode = auto`) داره که Repository Layer، پارسِ زمانِ زمان‌بند، روترِ دستوری، مهاجرتِ JSON→Postgres و اتصالِ دیتابیس رو پوشش می‌ده:

```bash
pip install -r requirements-dev.txt
pytest
```

---

## ۱۱) نکات امنیتی و مسئولیت‌پذیری

- فایلِ `.env` یا `SESSION_STRING` رو جایی منتشر نکنید - معادلِ رمزِ کاملِ اکانتتونه.
- دستورهای مدیریتیِ گروه (`.اخراج`/`.مسدود`/`.ارتقا`/`.تنزل`) رو فقط توی گروه‌های خودتون که ادمینشون هستید استفاده کنید.
- فاصله‌ی `CLOCK_INTERVAL` رو خیلی کم (زیرِ ۳۰ ثانیه) نکنید تا ریسکِ محدودیت از طرفِ تلگرام کم بمونه؛ همین‌طور `.ارسال‌خودکار` و `.برچسب‌همه` رو با احتیاط و فاصله‌ی معقول استفاده کنید.
- فایلِ خروجیِ `.پشتیبان تنظیمات` شاملِ رمز یا Session نیست، ولی متنِ یادداشت‌ها و تنظیماتِ منشی/ارسالِ‌خودکارتون توشه - جای امنی نگهش دارید.
- `.بازیابی` فقط با ریپلای‌کردنِ خودِ owner روی پیامِ outgoing کار می‌کنه، پس کسی جز خودتون نمی‌تونه از راه دور تنظیماتتون رو تغییر بده.
- `.پرسش`/`.خلاصه`/`.ترجمه‌هوشمند` و منشیِ AI متنِ پیام‌ها رو به یک سرویسِ بیرونی (OpenAI یا هر endpointی که تنظیم کردید) می‌فرستن - برای چت‌های حساس/خصوصی با احتیاط استفاده کنید.
- پلاگینِ نصب‌شده با سطحِ دسترسیِ کاملِ سلف‌بات اجرا می‌شه؛ فقط از منابعِ مطمئن نصب کنید.

---

## ۱۲) عیب‌یابی رایج

| مشکل | توضیح / راه‌حل |
|---|---|
| خطای `AuthKeyDuplicatedError` در لاگ | یعنی `SESSION_STRING` هم‌زمان از دو IP/جای مختلف استفاده شده و تلگرام باطلش کرده. ری‌استارتِ ساده حلش نمی‌کنه: با `generate_session.py` یک سشنِ جدید بسازید، مطمئن شوید همون سشن جای دیگه‌ای هم‌زمان اجرا نیست، و مقدارِ جدید رو جایگزین کنید. بات عمداً ۳۰۰ ثانیه قبل از خروج صبر می‌کنه تا فشارِ ری‌استارتِ پی‌درپی روی سرورهای تلگرام کم بشه. |
| `.قیمت` خطا می‌ده | ممکنه TGJU موقتاً در دسترس نباشه یا نیاز به فیلترشکن/DNS جایگزین داشته باشید؛ چند لحظه بعد دوباره امتحان کنید. |
| `.فیلترپورن`/`.پرسش`/منشیِ AI فقط پیامِ راهنما می‌دن | `AI_API_KEY` تنظیم نشده. |
| `.پنل` فقط لینکِ چتِ خصوصیِ بات کمکی رو می‌ده، نه پنلِ مستقیم | Inline Mode برای بات کمکی در BotFather فعال نشده (`/mybots` → بات → Bot Settings → Inline Mode). |
| پلاگینِ نصب‌شده بعدِ دیپلویِ جدید در Railway از بین رفته | Volume به `/data` وصل نیست؛ بخشِ [سیستم پلاگین](#۹-سیستم-پلاگین) رو ببینید. |
| فونتِ خودکار روی خودِ دستورها هم اعمال می‌شه | نباید بشه - هر چیزی که با پیشوندِ `.` شروع بشه از فونتِ خودکار مستثنی‌ست؛ اگر دیدید، احتمالاً `PREFIX` سفارشی با یکی از پترن‌های فونت تداخل داره. |
